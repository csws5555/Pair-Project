"""
Storefront API URL Configuration
NOTE: Temporarily disabled for migration creation
"""
from django.urls import path

app_name = 'storefront_api'

urlpatterns = [
    # API URLs will be re-enabled after fixing imports
]
